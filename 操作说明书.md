# 商品图片工作流操作说明书

## 需要准备

| 项目 | 是否必须 | 用途 |
| --- | --- | --- |
| Windows 10/11 64 位 | 必须 | 运行本程序 |
| Python 3.12 或更高版本 64 位 | 仅源码包需要 | 运行后端和安装依赖 |
| 挖象浏览器或 Microsoft Edge | 必须 | 打开商品页并使用店透视 |
| 店透视 | 必须采集时 | 在商品页下载主图、SKU 图和详情图 |
| 可用网络 | 必须 | 下载依赖、采集图片、调用模型 |
| API 配置 | 首次打开网页时输入视觉模型和生图模型 Key | 图片分析和生成 |

## 首次安装

1. 解压完整发布 ZIP 到一个普通文件夹，例如 `D:\商品图片工作流`。
2. 完整发布包已包含 Python、Playwright 和程序依赖，无需运行安装脚本。
3. 确认挖象浏览器或 Microsoft Edge 已安装。
4. 首次采集会打开采集浏览器。请在这个窗口中登录淘宝并确认店透视可以正常显示；浏览器档案固定保存在 `%LOCALAPPDATA%\ProductImageWorkflow\store-insight-profile`，升级程序后会继续复用。
5. 在网页右侧“采集浏览器”中选择“挖象浏览器”或“微软 Edge”，程序会自动检测安装路径。单链接和 Excel 批处理使用相同的浏览器选择。

`安装依赖.bat` 仅保留给源码包使用；它会创建虚拟环境、安装 `playwright>=1.55,<2` 和 Playwright Chromium 组件。

## 启动程序

1. 双击 `启动程序.bat`，它会启动本地服务并打开网页界面。
2. 程序没有命令行窗口，会直接打开本地网页界面。
3. 如果启动失败，程序会弹出中文错误提示，并在同目录生成 `startup_error.log`。

如果 `8765` 已被其他程序使用，程序会自动改用后续可用端口并打开正确地址。

## 完成一次任务

1. 在页面顶部的“商品链接”输入框粘贴淘宝、天猫、京东或抖音商品链接。
2. 选择本次需要生成的工作流：主图、SKU 图、详情图。直链采集始终完整采集三类图片和商品数据，工作流选择只控制后续生成类型。
3. 默认可直接使用对标商品第一张有效主图作为商品身份；需要替换为自己的商品时再上传一张产品图。
4. 点击开始采集或按页面引导确认任务。浏览器被打开后，如出现登录或验证，在该窗口中人工完成。
5. 采集完成后，系统按已选择的类型分析每张对标图片的构图、场景、卖点和图片结构，再调用图片模型生成对应数量的图片。
6. 页面显示完成后，点击“打开采集文件夹”或“打开生成文件夹”查看本机文件。图片不会在前端逐张铺开。

## 运行逻辑

`商品链接 -> 店透视快捷下载 -> 按类型整理素材 -> 视觉模型分析 -> 图片模型并发生成 -> 输出到文件夹`

- 主图默认生成 10 张；可切换为按对标数量或自定义数量。下拉选择会保存到后端，刷新页面或状态轮询不会恢复默认值。
- SKU 图和详情图同样只处理已采集到且被选择的图片类型。
- 没有采集到某一已选类型图片时，系统会提示补采，不会直接生成空结果。
- 每个类型下载完成后，程序会主动关闭店透视“文件下载完成”弹窗，再继续下一个类型；日志会显示图片包保存、弹窗关闭和新增图片数量。
- 聊天 Agent 用于理解补充需求和回答任务状态；商品链接仍应粘贴到独立的商品链接输入框。
- 在聊天中发送“只采集全部图片，不要生成”，系统会自动提交链接输入框中尚未提交的链接，采集主图、SKU 图和详情图，并停在采集完成阶段。

## 批量链接表格

1. 在“链接生图”中切换到“批量链接表格”，选择“参考对标商品创作”或“产品图批量替换”。
2. 参考创作表格使用“商品链接”“对标链接”或“URL”列；产品图替换表格需让每个商品配对一张我方产品图和一个对标链接，支持普通 Excel 嵌入图和 WPS `DISPIMG` 图片。
3. 程序按原表顺序逐行直接采集淘宝、天猫、京东或抖音商品，不进行搜同款，也不从第二款商品补采。
4. 点击“仅采集”时，只保存实际取得的图片、商品参数、可验证的 SKU 数据和原视频，不调用视觉模型、生图 API、OSS 或结果表格导出。

### 参考对标共享素材库

淘宝/天猫的“参考对标商品创作”会在采集前自动查询私有共享素材库。已有完整包时直接下载并导出当前 Excel，不重复调用采集器和生图模型；另一个客户端正在处理同一商品时，本行会提示锁占用并继续处理表格中的其他行。

OSS 查询、认证、网络或上传失败不会阻止本地任务，结果会保存到当前批次目录并标记为本地降级。只有主图 10 张、SKU 3–8 张、详情 6–15 张、三类任务全部完成且 Excel 已导出的默认结果才会发布到共享库。共享库不接入产品图批量替换、图片搜同款、自有商品、京东、抖音或快手任务。配置步骤见 [docs/oss-shared-library-setup.md](docs/oss-shared-library-setup.md)。
5. 点击“采集并生成”时执行完整链路；若之前已经仅采集，会复用有效素材继续生成。

部分抖音商品资料不提供可验证的 SKU 名称、颜色、规格、价格或 SKU 图片。程序会记录真实缺失状态，不会自动编造；需要准确 SKU 时可在产品图替换表格中补充 SKU 字段或 SKU 参考图。

## 图片搜同款表格

1. 点击“Excel 商品队列”中的“选择表格”，上传 `.xlsx` 文件。
2. 程序优先读取“1688商品图”字段下的嵌入图片；没有该字段时按表格中的嵌入图片顺序读取，空白行会跳过。
3. 点击“仅采集”只保存素材和数据；点击“采集并生成”执行淘宝搜同款、采集、生成和导出完整链路。
4. 每个淘宝商品采集主图、SKU 图、详情图、展开后的完整商品参数和原主视频 URL，再调用三条图片生成工作流。
5. 每个商品单独导出一个 Excel，包含总览、主图、详情图、SKU、商品参数、标题和视频。SKU 表包含规格、颜色和价格；标题表中的长标题为 55-60 字、短标题不超过 10 字；视频表中的 URL 可直接点击打开。
6. 点击“打开文件夹”查看本批次的商品目录、生成图片和独立 Excel。

批处理采集按商品逐个限频执行；单个商品内部的图片分析与生成按照该商品素材数量并发。点击批处理区的“停止”后，程序结束当前采集子进程或取消剩余生成任务，不会继续下一行。

## 采集浏览器

点击右侧“采集浏览器”，在“挖象浏览器”和“微软 Edge”中二选一。选择后程序会自动检测安装位置并保存，后续单链接采集和 Excel 批处理均使用该浏览器。

程序会优先检测常见安装目录，并读取 Windows 已安装程序信息。未安装所选浏览器时，页面会提示安装后重新选择。

## 停止任务

- 点击“停止采集”会结束当前采集进程，已下载的图片保留。
- 停止采集或采集失败后任务会保持暂停，不会自动重复打开浏览器；处理问题后在聊天中发送“继续采集”才会重试。
- 点击“停止生成”会请求取消尚未完成的生成任务，已经完成的图片保留。
- 停止后可重新粘贴链接、选择类型并发起新任务。

## 常见问题

| 现象 | 处理方式 |
| --- | --- |
| 双击启动脚本后提示未找到虚拟环境 | 先运行 `安装依赖.bat`。 |
| 提示找不到 Chrome/Edge | 安装 Chrome 或 Edge 后重新开始采集。 |
| 浏览器打开了但没有店透视下载入口 | 在当前采集浏览器窗口安装、启用并登录店透视扩展，然后重新采集。 |
| 店透视打开了“自定义下载” | 脚本会先按图片类型点击快捷入口；只有主图快捷入口未匹配或未触发下载时才使用自定义下载兜底。 |
| 采集过程停在登录或验证 | 在弹出的浏览器内完成登录/验证；脚本最长等待 10 分钟，期间不会点击、滚动或刷新页面。 |
| 只生成主图但采集目录还有其他图片 | 这是正常行为。直链采集会完整保存三类素材，工作流选择只限制生成。 |
| 生成失败 | 检查网络和 `local_settings.json` 中的 API 地址、图片生成 Key、视觉模型 Key 是否可用。 |
| 找不到结果 | 在页面点击“打开采集文件夹”或“打开生成文件夹”；所有结果位于 `outputs`。 |

## API 配置

完整便携包已包含不带密钥的本机配置，可直接启动；只有源码运行时才需要将 `local_settings.example.json` 复制为 `local_settings.json` 并填写 API 地址与 OSS 配置。首次打开网页时会自动显示“配置模型 API”弹窗，同时输入视觉模型 API Key 和生图模型 API Key 后保存。两个 Key 会写入本机 `local_settings.json`，刷新页面或重启服务后自动恢复；需要更换时点击页面顶部的“模型 API”状态入口。Key 不会出现在状态接口或前端构建文件中，`local_settings.json` 已被 Git 忽略。
